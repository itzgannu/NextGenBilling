<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:innerRadius="@dimen/small_0"
    android:shape="ring"
    android:thicknessRatio="2"
    android:useLevel="false" >

    <solid
        android:color="@android:color/transparent" />

    <padding
        android:left="@dimen/base"
        android:right="@dimen/base"
        android:top="@dimen/base"
        android:bottom="@dimen/base"/>

    <stroke
        android:width="@dimen/small_2"
        android:color="@android:color/black" />

</shape>